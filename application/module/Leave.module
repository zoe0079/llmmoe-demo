<?php
/**
 * 机器需要有自身的目的，才能有感情。
 * 这个目的就像人类自认为人类存在的意义，他才会害怕、才会高兴、才会有爱。
 * 但这个问题，人类自身没有一点认识，如何去设计机器。
 * 对这个问题，人类永远无法站在客观的角度，
 * 是上帝的设计，在无法交流的前提下，人类只能去揣测，
 * 就像机器永远想不通自己存在的意义一样，只有人类知道。
 * 这就像个递归的问题。
 *
 * Created by PhpStorm.
 * User: llmmoe
 * Date: 2018/4/7
 * Time: 20:45
 */

namespace app\module;


class Leave extends \sys\lib\Module {
    /**
     * Leave constructor.
     * @throws \Exception
     */
    function __construct() {
        parent::__construct();
    }

    /**
     * @param array $data
     * @param string $table
     * @return array
     * @throws \Exception
     */
    function find($data = array(), $table = 'leave') {
        return parent::find($data, $table); // TODO: Change the autogenerated stub
    }

    /**
     * @param $data
     * @return array|bool|\mysqli_result
     * @throws \Exception
     */
    function insert($data) {
        $data['createtime'] = date('Y-m-d H-i-s');
        $this->db->query($this->sql->insert("leave", $data));
    }
}